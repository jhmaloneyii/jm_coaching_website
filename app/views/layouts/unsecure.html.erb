<!DOCTYPE html>
<html>
<head>

<%= render 'shared/head' %>

</head>
<body id="page-top">
  
  <%= render 'shared/nav' %>  

  <%= render 'shared/secure/admin_sidebar' if admin_signed_in? %>

  <%= yield %>

  <%= render 'shared/footer' %>

</body>
</html>
