<!DOCTYPE html>
<html>
<head>

<%= render 'shared/head' %>

</head>
<body>

  <%= render 'shared/nav' %>

  <div class="wrapper">

    <%= render 'shared/secure/admin_sidebar' if admin_signed_in? %>

    <div class="container-fluid" id="yield-content">
      <%= yield %>
      <%= render 'shared/footer' %>
    </div>

  </div>

</body>
</html>
