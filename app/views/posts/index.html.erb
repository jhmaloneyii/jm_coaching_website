<section id="about" class="content-section">
      <div class="container">
        <div class="row">
          <div class="col-lg-10 mx-auto">
            <% @posts.each do |post| %>
                 <h1><%= link_to "#{post.title}", post %></h1>
                 <p><%= post.content %></p>
            <% end %>
          </div>
        </div>
      </div>
</section>

